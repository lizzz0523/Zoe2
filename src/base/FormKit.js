/*
 * 继承自View，是表单（form）组件大类的基类，
 * 对外提供了表单组件的几个基本接口，分别是name，value，fill，reset，error，enable，disable，
 * FormKit类组件可以与form组件结合使用，提供完整的表单功能
 *
 * 直接继承自FormKit的组件
 * - radio
 * - date
 * - color
 * - input
 * - select
 * - multi-select
 * - radio
 * - radio-group
 * - check
 * - check-group
 * - fileupload
 */